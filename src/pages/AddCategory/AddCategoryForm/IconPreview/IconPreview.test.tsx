import IconPreview from './IconPreview';

describe('<IconPreview />', () => {

  it('shoulde render img', () => {
    //
  });
  
  // it('should render img without class imgError', async () => {
  //   render(
  //     <RecoilRoot>
  //       <BrowserRouter>
  //         <AddCategoryForm />
  //       </BrowserRouter>
  //     </RecoilRoot>
  //   );

  //   const validIconLink = 'img';

  //   const categoryNameInput = screen.getByLabelText('How would like to call this Category?');
  //   const typeInput = screen.getByLabelText('Type:');
  //   const iconInput = screen.getByLabelText('Icon:');
  //   fireEvent.change(categoryNameInput, { target: { value: validCategory.value } });
  //   fireEvent.change(typeInput, { target: { value: validCategory.type } });
  //   fireEvent.change(iconInput, { target: { value: validIconLink } });


  //   await act(async () => {
  //     await Promise.resolve();
  //   });

  //   const img = await screen.findByAltText('icon of the category');

  //   await waitFor(() => expect(img).toHaveClass('imgError'));

  // });

  // it('should render icon with first letter of the Name when link is broken and category name is given', () => {
  //   //
  // });

  // it('should render icon with text error when link is broken and no icon is provided', () => {
  //   //
  // });

});